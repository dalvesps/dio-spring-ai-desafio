1. Na pasta `config/`, crie `SpringAiConfig.java` registrando os Beans do ChatClient.
2. Na pasta `service/`, crie `OrcamentoIaService.java` que utiliza o `ChatClient` com um System Prompt customizado, registrando as Tools da pasta `tools/` e processando o texto enviado para gerar a resposta final ao usuário.
