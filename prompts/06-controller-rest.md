Na pasta `src/main/java/com/dio/orcamento/controller/`:
1. Crie o DTO `dto/RespostaOrcamentoDTO.java` (texto transcrito, ação executada e resposta da IA).
2. Crie o `@RestController` `OrcamentoAudioController.java` com o endpoint `POST /api/v1/orcamento/audio`, unindo `AudioTranscriptionService` e `OrcamentoIaService`.
